import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <div className='header'>
        <button className='login'>Login</button>
    </div>
  );
};

export default Header;
